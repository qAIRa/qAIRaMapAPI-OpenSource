name: Tests

on: [push]

jobs:
  run-tests:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout 🛎️
        uses: actions/checkout@v2

      - name: Build test container 🔧
        run: |
          make build-test

      - name: Run tests 🚀
        run: |
          make test
