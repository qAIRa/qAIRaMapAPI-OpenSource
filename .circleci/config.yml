version: 2.1

jobs:
  run-tests:
    docker:
      - image: cimg/base:2021.10
    steps:
      - checkout
      - setup_remote_docker

      - run:
          name: Build test container 🔧
          command: |
            make build-test

      - run:
          name: Run tests 🚀
          command: |
            make test

workflows:
  version: 2
  test:
    jobs:
      - run-tests
